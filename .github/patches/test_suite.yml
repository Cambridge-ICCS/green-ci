         with:
           persist-credentials: false
         uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
-
-      - name: Test static analysis template
-        run: |
-          pip install -r requirements.txt
-          copier copy -a .github/answers/static_analysis.yml -f . .
-          git diff .github/workflows/static_analysis.yml | tail -n +6 > tmp.patch
-          if diff tmp.patch .github/patches/static_analysis.yml; then
-            exit 1
-          fi
-          rm tmp.patch
-
-      - name: Test test suite template
-        run: |
-          copier copy -a .github/answers/test_suite.yml -f . .
-          git diff .github/workflows/test_suite.yml | tail -n +6 > tmp.patch
-          if diff tmp.patch .github/patches/test_suite.yml; then
-            exit 1
-          fi
-          rm tmp.patch
+      - name: Test suite
+        run: echo "TODO replace this echo call with your test suite commands"
\ No newline at end of file
